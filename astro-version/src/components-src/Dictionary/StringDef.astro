---
import HoverDefinition from '../../HoverDefinition'
---
  <HoverDefinition
    definition="A string is a piece of human text your computer knows isn't code, but it can still be used in your code"
    {...Astro.props}
  >
    {Astro.props.children || 'string'}
  </HoverDefinition>