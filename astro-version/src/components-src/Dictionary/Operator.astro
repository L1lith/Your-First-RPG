---
import HoverDefinition from '../../HoverDefinition'
---
<HoverDefinition
  definition='An operator is an action in coding that inputs and outputs data, like comparing two booleans, or adding two numbers'
  {...Astro.props}>
  {Astro.props.children || 'operator'}
</HoverDefinition>
