---
import HoverDefinition from '../../HoverDefinition'
---
<HoverDefinition
    definition="The syntax is the grammer (or rules) of the programming language"
    {...Astro.props}>
    {Astro.props.children || 'syntax'}
  </HoverDefinition>
